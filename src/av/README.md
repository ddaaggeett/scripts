# Node.js Audio Recording Script

This Node.js script [record_audio.js](./record_audio.js) utilizes the `fluent-ffmpeg` library to record audio and save it to a file with a timestamp in the filename.
